{{- if and .Values.ecrPullSecret.enabled .Values.ecrPullSecret.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.ecrPullSecret.serviceAccount.name }}
  namespace: {{ .Values.namespace }}
{{- end }}