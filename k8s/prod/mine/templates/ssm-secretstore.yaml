apiVersion: external-secrets.io/v1beta1
kind: SecretStore
metadata:
  name: aws-ssm-store
  namespace: mine-app-test
spec:
  provider:
    aws:
      service: ParameterStore
      region: ap-northeast-2
